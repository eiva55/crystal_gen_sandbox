model:
  _target_: sandbox.models.adit.ADiTModel
  ckpt_path: ./models/adit/checkpoints/adit/ldm.ckpt
  autoencoder_ckpt: ./models/adit/checkpoints/adit/vae.ckpt
  data: mp20_only
  sampling:
    num_samples: 10
    batch_size: 10
task:
  _target_: sandbox.tasks.generation.CrystalGenerationTask
dataset:
  _target_: sandbox.datasets.mp20.MP20Dataset
runner:
  _target_: sandbox.runners.generate.run_generation
  num_samples: 10
  batch_size: 10
  device: cpu
  save_dir: ./outputs
mode: generate
seed: 42
